## Alien Calculator (React App)

### Installation
 - Install Node in your PC
 - Run `npm install`
 - Wait for packages to install
 - Configure the backend URL in .env file
 - Run `npm start`
 - Browse the development server URL